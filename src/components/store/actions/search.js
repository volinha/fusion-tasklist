const SearchTextUpdate = (value) => {
    return{
        type: 'UPDATE_SEARCH',
        value
    }
}

// eslint-disable-next-line import/no-anonymous-default-export
export default {
    SearchTextUpdate
}